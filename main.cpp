//Avleen Gill - COMSC 210
//Lab 32: Toll Booth

#include <iostream>
#include <deque>
#include "Car.h"
using namespace std;

const int ISIZE = 2; //represents cars already in line 

int main() {

    deque<Car> booth;

    for (int i = 0; i < ISIZE; i++) {
        booth.push_back(Car()); //call Car -> calls constructor to generate random car
    }

    for (int numcars = 0; numcars.)


    return 0;
}